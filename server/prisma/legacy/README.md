# Legacy SQL

Files in this directory are retained for historical reference only. They are
not part of the executable Prisma migration chain.

`005_add_font_family.sql` targeted the old singular `admin` table. The
canonical migration is now
`../migrations/20260125210000_add_font_family/migration.sql`.
