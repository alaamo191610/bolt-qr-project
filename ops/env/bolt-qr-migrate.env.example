# Install as /etc/bolt-qr/bolt-qr-migrate.env with owner root:root and mode 0600.
# Load only for deployment/migration commands. The Node service must never read this file.
MIGRATION_DATABASE_URL=postgresql://boltqr_migrate:REPLACE_ME@127.0.0.1:5432/restaurant_db?schema=public
