# Install as /etc/bolt-qr/bolt-qr.env with owner root:boltqr and mode 0640.
NODE_ENV=production
HOST=127.0.0.1
PORT=3000
TRUST_PROXY_HOPS=1
UPLOAD_DIR=/var/lib/bolt-qr/uploads
SHUTDOWN_TIMEOUT_MS=25000

# Replace every placeholder. Keep this file outside the repository.
DATABASE_URL=postgresql://boltqr:REPLACE_ME@127.0.0.1:5432/restaurant_db?schema=public
JWT_SECRET=REPLACE_WITH_AT_LEAST_32_RANDOM_BYTES
# Generate independently: openssl rand -hex 32
SUPER_ADMIN_MFA_ENCRYPTION_KEY=REPLACE_WITH_64_HEX_CHARACTERS
TOKEN_ISSUER=https://api.example.com
CORS_ORIGINS=https://example.com

# Use a server-project DSN. Browser telemetry uses the separate build environment template.
SENTRY_DSN=https://REPLACE_PUBLIC_KEY@REPLACE_INGEST_HOST/REPLACE_PROJECT_ID
SENTRY_ENVIRONMENT=pilot
SENTRY_TRACES_SAMPLE_RATE=0
UPTIME_HEALTH_URL=https://example.com/api/health/ready
